# AWS Cognito Configuration
AWS_REGION=us-east-1
COGNITO_USER_POOL_ID=us-east-1_WuzfoISRw
COGNITO_CLIENT_ID=6h307650vrg74mdgc5m182jg99
COGNITO_CLIENT_SECRET=your_cognito_client_secret_here
COGNITO_DOMAIN=https://us-east-1wuzfoisrw.auth.us-east-1.amazoncognito.com

# Google OAuth through Cognito
COGNITO_HOSTED_UI_DOMAIN=us-east-1wuzfoisrw.auth.us-east-1.amazoncognito.com
# Production redirect URI (used as fallback if COGNITO_REDIRECT_URI_PRODUCTION is not set)
COGNITO_REDIRECT_URI=https://simple-chat-interface-production.up.railway.app/auth/cognito/callback
# Optional: Environment-specific redirect URIs (automatically detected from request origin if not set)
COGNITO_REDIRECT_URI_PRODUCTION=https://simple-chat-interface-production.up.railway.app/auth/cognito/callback
COGNITO_REDIRECT_URI_STAGING=https://simple-chat-interface-staging.up.railway.app/auth/cognito/callback

# Session Configuration
SESSION_SECRET=91083034e07905850ded459a261c62f384715b3b3014cc6b01150a324969ff21

# Domain Restriction
ALLOWED_DOMAIN=kyocare.com


# Amazon S3 Configuration
# Bucket to use for file uploads and S3 Admin Panel listings
S3_BUCKET_NAME=your_s3_bucket_name_here
# Optional: Prefix within the bucket for ChatKit-managed uploads
S3_CHATKIT_UPLOAD_PREFIX=chatkit-uploads
# Optional: Maximum file size in bytes (default: 20MB)
S3_CHATKIT_MAX_FILE_BYTES=20971520
# Optional: Presigned upload URL lifetime in seconds (default: 15 minutes)
S3_CHATKIT_UPLOAD_URL_TTL=900
# Optional: Presigned download URL lifetime in seconds (default: 1 hour)
S3_CHATKIT_DOWNLOAD_URL_TTL=3600


# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_CHATKIT_WORKFLOW_ID=your_chatkit_workflow_id_here
OPENAI_CHATKIT_PUBLIC_KEY=your_chatkit_public_key_here
# Optional: Local development domain key (for localhost)
OPENAI_CHATKIT_PUBLIC_KEY_LOCAL=your_local_chatkit_public_key_here

# Optional: Python ChatKit proxy (FastAPI) - set to enable proxy path
# Example: http://localhost:8000
PYTHON_CHATKIT_URL=
PY_BACKEND_URL=

# Logging Configuration
# LOGGER_TYPE is auto-detected based on available database variables:
# - If PostgreSQL variables (PGHOST, PGDATABASE, etc. or DB_HOST, DB_NAME, etc.) are present -> uses PostgreSQL
# - Otherwise defaults to 'file' logging
# LOGGER_TYPE=postgresql
# Options: 'file', 'postgresql'

# File-based Logging (when no database is available)
LOG_DIR=./logs
MAX_FILE_SIZE=10485760
MAX_FILES=5

# PostgreSQL Database Logging (Railway PostgreSQL)
# Railway automatically provides these environment variables:
# DB_HOST=postgres.railway.internal
# DB_PORT=5432
# DB_NAME=railway
# DB_USER=postgres
# DB_PASSWORD=auto-generated
# DB_SSL=true